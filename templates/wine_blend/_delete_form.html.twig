<form method="post" action="{{ path('app_wine_blend_delete', {'id': wine_blend.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wine_blend.id) }}">
    <button class="btn">Delete</button>
</form>
